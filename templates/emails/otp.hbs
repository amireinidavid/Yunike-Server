<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Your Yunike Verification Code</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap');
    
    :root {
      --primary: #4f46e5;
      --primary-dark: #4338ca;
      --primary-light: #818cf8;
      --secondary: #06b6d4;
      --gray-light: #f9fafb;
      --gray: #e5e7eb;
      --gray-dark: #6b7280;
      --text-dark: #111827;
      --success: #10b981;
      --shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
    }
    
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }
    
    body {
      font-family: 'Poppins', Arial, sans-serif;
      line-height: 1.6;
      color: var(--text-dark);
      background-color: #f5f5f5;
      max-width: 100%;
      margin: 0;
      padding: 0;
    }
    
    .wrapper {
      width: 100%;
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    
    .container {
      background-color: #ffffff;
      border-radius: 12px;
      box-shadow: var(--shadow);
      overflow: hidden;
      margin: 20px 0;
    }
    
    .header {
      background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
      padding: 30px 20px;
      text-align: center;
      color: white;
    }
    
    .header h1 {
      font-size: 28px;
      font-weight: 700;
      margin-bottom: 10px;
    }
    
    .header p {
      opacity: 0.9;
      font-weight: 300;
    }
    
    .content {
      padding: 30px;
    }
    
    .greeting {
      font-size: 20px;
      margin-bottom: 15px;
      font-weight: 600;
    }
    
    .message {
      margin-bottom: 25px;
      color: var(--gray-dark);
    }
    
    .code-container {
      text-align: center;
      margin: 30px 0;
    }
    
    .code {
      display: inline-block;
      font-size: 32px;
      font-weight: 700;
      letter-spacing: 8px;
      background: var(--gray-light);
      color: var(--primary);
      padding: 20px 30px;
      border-radius: 10px;
      border: 2px dashed var(--primary-light);
      box-shadow: 0 3px 10px rgba(0,0,0,0.05);
      min-width: 220px;
    }
    
    .expiry {
      text-align: center;
      font-size: 14px;
      color: #ef4444;
      margin: 10px 0 25px;
    }
    
    .timer {
      display: inline-block;
      font-weight: 600;
    }
    
    .details {
      background-color: var(--gray-light);
      border-left: 4px solid var(--primary);
      padding: 15px;
      margin: 25px 0;
      font-size: 14px;
      color: var(--gray-dark);
    }
    
    .footer {
      background-color: var(--gray-light);
      padding: 20px;
      text-align: center;
      font-size: 13px;
      color: var(--gray-dark);
      border-top: 1px solid var(--gray);
    }
    
    .footer p {
      margin: 5px 0;
    }
    
    .highlight {
      color: var(--primary);
    }
    
    .divider {
      height: 1px;
      background-color: var(--gray);
      margin: 20px 0;
    }
    
    @media screen and (max-width: 480px) {
      .wrapper {
        padding: 10px;
      }
      
      .content {
        padding: 20px;
      }
      
      .code {
        font-size: 24px;
        letter-spacing: 6px;
        padding: 15px 20px;
      }
    }
  </style>
</head>
<body>
  <div class="wrapper">
    <div class="container">
      <div class="header">
        <h1>Verification Code</h1>
        <p>Secure your {{action}} with this one-time code</p>
      </div>
      
      <div class="content">
        <p class="greeting">Hello {{name}},</p>
        <p class="message">Thank you for using Yunike! To complete your {{action}}, please use the verification code below:</p>
        
        <div class="code-container">
          <div class="code">{{otpCode}}</div>
        </div>
        
        <p class="expiry">
          <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" viewBox="0 0 16 16" style="vertical-align: middle; margin-right: 4px">
            <path d="M8 3.5a.5.5 0 0 0-1 0V9a.5.5 0 0 0 .252.434l3.5 2a.5.5 0 0 0 .496-.868L8 8.71V3.5z"/>
            <path d="M8 16A8 8 0 1 0 8 0a8 8 0 0 0 0 16zm7-8A7 7 0 1 1 1 8a7 7 0 0 1 14 0z"/>
          </svg>
          This code will expire in <span class="timer">{{expiryMinutes}} minutes</span>
        </p>
        
        <div class="details">
          <p>• Do not share this code with anyone</p>
          <p>• Yunike will never ask for your password or full credit card details</p>
          <p>• If you did not request this code, please ignore this email or contact our support</p>
        </div>
        
        <div class="divider"></div>
        
        <p style="font-size: 14px; text-align: center;">Need help? <a href="#" style="color: var(--primary); text-decoration: none; font-weight: 500;">Contact our Support Team</a></p>
      </div>
      
      <div class="footer">
        <p>This is an automated message from <span class="highlight">Yunike</span>. Please do not reply to this email.</p>
        <p>&copy; {{currentYear}} Yunike. All rights reserved.</p>
      </div>
    </div>
  </div>
</body>
</html> 