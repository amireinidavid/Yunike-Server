# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @org/org`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t @org/org`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system @org/org && \
          adduser --system -G @org/org @org/org

COPY apps/org/dist @org/org/
RUN chown -R @org/org:@org/org .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix @org/org --omit=dev -f install

CMD [ "node", "@org/org" ]
